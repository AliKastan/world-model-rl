# Paper Figures

Run the comparison experiment to generate publication-quality figures:

```bash
python -m training.compare --episodes 10000 --seeds 3 --output experiments/results/main/ --figures
```

Figures will be saved here as PDF and PNG (300 DPI).
